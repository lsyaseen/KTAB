language: cpp
 
compiler:
   - gcc
   
addons:
    apt:
      sources:
        - george-edison55-precise-backports # cmake 3.2.3
        - ubuntu-toolchain-r-test
        
      packages:
        - g++-6
        - cmake
        - cmake-data
        - libfltk1.3-dev
        
 before-script:      
 - git clone https://github.com/leethomson/tinyxml2.git ~/build/KAPSARC
 
script:
    ./reconfig.sh
