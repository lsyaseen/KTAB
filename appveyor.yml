version: "{build}"

os: Visual Studio 2015

platform:
  - Win32
  - x64

configuration:
  - Debug
  - Release

# scripts that need to be run prior to repo cloning...
init:
  - cmd: cmake --version

branches:
  - except:
    - gh-pages  
